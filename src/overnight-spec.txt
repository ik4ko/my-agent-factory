CRITICAL SYSTEM UPGRADE: CORE-STORE ZUSTAND BRIDGE & HOVER RETICLE
------------------------------------------------------------------
We are completing the state bridge highlighted in our architectural review. Copilot flagged that <CinematicCore /> has focusTarget={null} hardcoded, and the 3D laser vector beam has no producer data. 

Execute the following three steps with maximum type safety and zero placeholder code:

1. CREATE THE STATE STORE (src/lib/fx/core-store.ts)
   - Initialize a strict Zustand store named `useCoreFxStore`.
   - Implement these state tracking values:
     * `isListening`: boolean
     * `focusedAgentId`: string | null
     * `focusTarget`: { x: number; y: number } | null
   - Implement atomic actions:
     * `setIsListening(status: boolean)`
     * `setFocusTarget(id: string | null, rect: DOMRect | null)`
   - Inside `setFocusTarget`, if a rect is provided, calculate the exact WebGL Normalized Device Coordinates (NDC) center point of that agent card container so the 3D engine can target it.

2. ATTACH THE HOVER PRODUCER (src/components/dashboard/agent-fleet.tsx)
   - Open the agent fleet component. Ensure every agent card container uses a React ref to track its position.
   - Implement a debounced mouse-enter listener using requestAnimationFrame or a 100ms debounce window to prevent rapid 'hover-flicker' or layout thrashing.
   - On mouse enter, call `setFocusTarget(agentId, cardRef.current.getBoundingClientRect())`. On mouse leave, call `setFocusTarget(null, null)` so the 3D laser beam snaps back to its idle core beautifully.

3. WIRE THE CONSUMER (src/components/dashboard/dashboard-client.tsx)
   - Open the primary dashboard layout file. Remove the hardcoded `focusTarget={null}` from the <CinematicCore /> instantiation lines.
   - Pull the live tracking state using an atomic Zustand selector: 
     `const focusTarget = useCoreFxStore(state => state.focusTarget);`
   - Feed that live selector straight into <CinematicCore focusTarget={focusTarget} /> so the 3D particle dust and laser vector line lean dynamically toward whichever card the user hovers over.

Run full TypeScript compilation check checks when done. Do not leave any hanging mock variables or duplicate layout wrappers.