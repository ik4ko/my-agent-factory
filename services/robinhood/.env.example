# services/robinhood's OWN env — never copy these into the Next app's .env.local.
RH_USERNAME=
RH_PASSWORD=
# Optional: base32 TOTP secret if 2FA is app-based, so login never blocks on
# an interactive MFA prompt. Leave unset to be prompted on first run instead.
RH_MFA_SECRET=
